#!/usr/bin/env bash
set -euo pipefail

codespell
yamllint .
yamale -s schemas/rule.schema.yaml rules
pytest -q tests/unit/test_analysis_profiles.py -k repository_profile_examples

markdownlint-cli2 "**/*.md"
